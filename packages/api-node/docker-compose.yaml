version: '3.8'
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: sa
      POSTGRES_PASSWORD: 1Senha!.
      POSTGRES_DB: database
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
